# Deferred Improvements

No discretionary upgrades are currently tracked. The capabilities in `README.md` are the planned greenfield implementation, not upgrades to a shipped system.

Use permanent identifiers in the form `FU-001`. Implemented entries move to `docs/archive/COMPLETED-UPGRADES.md`.
